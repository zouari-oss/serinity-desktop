<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.control.ProgressBar?>
<?import javafx.scene.control.Separator?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.VBox?>

<VBox xmlns="http://javafx.com/javafx/17.0.2"
      xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="com.serinity.sleepcontrol.controller.ReveCardController"
      styleClass="card"
      stylesheets="@../styles/styles.css"
      maxWidth="280.0"
      prefWidth="280.0"
      spacing="8.0">

    <padding>
        <Insets bottom="12.0" left="12.0" right="12.0" top="12.0" />
    </padding>

    <!-- En-tête : titre + type (comme date + qualité) -->
    <HBox alignment="CENTER_LEFT" spacing="10.0">
        <Label fx:id="titreLabel" styleClass="card-title" wrapText="true" />
        <!-- badge type de rêve, même style que qualiteLabel -->
        <Label fx:id="typeLabel" styleClass="type-label" />
    </HBox>

    <Separator />

    <!-- Description -->
    <Label fx:id="descriptionLabel" styleClass="card-info" wrapText="true" />

    <!-- Humeur -->
    <Label fx:id="humeurLabel" styleClass="card-info" />

    <!-- Intensité -->
    <HBox alignment="CENTER_LEFT" spacing="5.0">
        <Label styleClass="card-info" text="Intensité: " />
        <ProgressBar fx:id="intensiteBar" prefWidth="90.0" />
        <Label fx:id="intensiteValue" styleClass="card-info" />
    </HBox>

    <!-- Anxiété -->
    <Label fx:id="anxieteLabel" styleClass="card-info" />

    <!-- Badges -->
    <HBox alignment="CENTER_LEFT" spacing="5.0">
        <Label fx:id="couleurBadge" text="Couleur"
               styleClass="badge, badge-couleur"
               visible="false" managed="false" />

        <Label fx:id="recurrentBadge" text="Récurrent"
               styleClass="badge, badge-recurrent"
               visible="false" managed="false" />

        <Label fx:id="lucideBadge" text="Lucide"
               styleClass="badge, badge-lucide"
               visible="false" managed="false" />
    </HBox>

    <!-- Émotions (optionnel) -->
    <Label fx:id="emotionsLabel"
           styleClass="card-info-secondary"
           managed="false" visible="false"
           wrapText="true" />

    <!-- Boutons d'action (mêmes classes que Sommeil) -->
    <HBox alignment="CENTER" spacing="8.0">
        <Button fx:id="btnVoir"     text="Voir"      styleClass="btn-primary, btn-sm" />
        <Button fx:id="btnAnalyse"  text="🧠 IA"     styleClass="btn-secondary, btn-sm" />
        <Button fx:id="btnModifier" text="Modifier"  styleClass="btn-warning" />
        <Button fx:id="btnSupprimer" text="X"        styleClass="btn-danger" />
    </HBox>


</VBox>