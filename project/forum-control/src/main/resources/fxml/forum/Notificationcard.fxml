<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<HBox xmlns="http://javafx.com/javafx"
      xmlns:fx="http://javafx.com/fxml"
      fx:id="root"
      fx:controller="com.serinity.forumcontrol.Controllers.NotificationCardController"
      spacing="10"
      alignment="CENTER_LEFT"
      style="-fx-padding: 12; -fx-background-color: white; -fx-cursor: hand; -fx-border-color: #e0e0e0; -fx-border-width: 0 0 1 0;">

    <!-- Notification Icon -->
    <Label fx:id="iconLabel"
           text="🔔"
           style="-fx-font-size: 20;"/>

    <!-- Notification Content -->
    <VBox spacing="5" HBox.hgrow="ALWAYS">
        <Label fx:id="contentLabel"
               wrapText="true"
               style="-fx-font-size: 13; -fx-text-fill: #333;"/>

        <Label fx:id="dateLabel"
               style="-fx-font-size: 11; -fx-text-fill: #999;"/>
    </VBox>

    <!-- Unseen Indicator -->
    <Label fx:id="unseenIndicator"
           text="●"
           style="-fx-text-fill: #2196F3; -fx-font-size: 16;"
           visible="false"/>

    <!-- Delete Button -->
    <Button fx:id="deleteButton"
            text="✖"
            onAction="#onDelete"
            style="-fx-background-color: transparent; -fx-text-fill: #999; -fx-font-size: 14; -fx-cursor: hand; -fx-padding: 0 5;"/>

</HBox>
